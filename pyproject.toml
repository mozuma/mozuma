[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.pyright]
include = ["src", "tests"]

[tool.isort]
profile = "black"

[tool.darker]
src = ["src", "tests"]
isort = true
